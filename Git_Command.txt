some usefull git command 

create new branch
	git checkout -b map
	(this will move the current repo into map branch called map)

push the new branch to the repo
	git push origin map

check all the branch we have
	git branch

PUSH code into branch
	git push --upstream-origin map

check the repo is clean or not 
	git status

change to branch
	git checkout master